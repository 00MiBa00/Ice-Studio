import 'package:flutter/cupertino.dart';

class AppColors {
  // Primary palette - Blue / Light Blue / White
  static const Color primaryBlue = Color(0xFF0A7AFF);
  static const Color lightBlue = Color(0xFF64D2FF);
  static const Color accentBlue = Color(0xFF5AC8FA);
  
  // Backgrounds
  static const Color darkBackground = Color(0xFF000000);
  static const Color surfaceDark = Color(0xFF1C1C1E);
  static const Color surfaceLight = Color(0xFF2C2C2E);
  
  // Light mode
  static const Color lightBackground = Color(0xFFF2F2F7);
  static const Color surfaceLightMode = Color(0xFFFFFFFF);
  
  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8E93);
  
  // Mode-specific colors
  static const Color antiStress = Color(0xFF0A7AFF);
  static const Color focusBounce = Color(0xFF5AC8FA);
  static const Color zenSand = Color(0xFF64D2FF);
}

name: ice_studio
description: Flutter application

version: 1.0.0+1 # Версия приложения + номер сборки

environment:
  sdk: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  webview_flutter_wkwebview: ^3.23.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
 